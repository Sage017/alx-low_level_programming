#include <stdio.h>

/**
 * main - Prints the string puz
 *
 * Return: 0 for success, 1 otherwise
 */
int main(void)
{
	char puz[] = "\"Programming is like building a multilingual puzzle";

	puts(puz);
	return (0);
}
