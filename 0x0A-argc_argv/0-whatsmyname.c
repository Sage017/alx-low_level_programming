#include "main.h"

/**
 * main - prints its name, followed by a new line.
 * @argc: count of command-line arguments
 * @argv: array of strings containing arguments
 *
 * Return: Always 0, non-zero if error
 */
int main(int argc, char *argv[])
{
	int i = 0;

	(void) argc;
	(void) argv;

	printf("%s\n", argv[i]);
	return (0);
}
